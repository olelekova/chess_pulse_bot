# macOS
.DS_Store

# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
*.egg-info/

# Локальные claude-code worktrees — не для публичного репо
.claude/

# Архивы и большие медиа, не нужные на проде
files.zip

# Легаси-копия бота — у тебя в папке лежит `chess_pulse_bot/` с копиями
# bot.py и др. Это дубль, прод его не использует, в репо тащить не надо.
chess_pulse_bot/

# Скиллы (плагины) — локальные, не на проде
skills/

# Мой тестовый скрипт — оставляю в .gitignore по умолчанию;
# если хочешь его в репо, удали эту строку
test_preview.py

# Виртуальные окружения, если будут
venv/
.venv/
env/

# IDE / редакторы
.idea/
.vscode/
*.swp
